SET PATH TO C:\SUERUT\EMPRE1
SET EXCLUSIVE off
SET DELETED ON
CLOSE tables
SELECT 0
USE 42021 ALIAS sueldos
reemp()
*s.legajo = p.legajo WHERE ALLTRIM(orden) = "20210311" .and. concepto = 20 INTO CURSOR LISTA
SELECT p.legajo as l ,p.nombre,s.legajo,s.tipoconcep FROM sueldos s INNER JOIN personal p ON ;
s.legajo = p.legajo WHERE val(s.tipoconcep) = 80 into cursor lista

browse
DELETE FROM  sueldos
SELECT LISTA
SCAN
     SELECT sueldos
     RECALL FOR sueldos.LEGAJO = LISTA.L .AND. sueldos.LIQUIDA = 2
     SELECT LISTA

ENDSCAN


*****************
FUNCTION reemp
****************
REPLACE TIPOCONCEP WITH "80" FOR LEGAJO = 863 .AND. LIQUIDA = 2
REPLACE TIPOCONCEP WITH "80" FOR LEGAJO = 1   .AND. LIQUIDA = 2
REPLACE TIPOCONCEP WITH "80" FOR LEGAJO = 173 .AND. LIQUIDA = 2
REPLACE TIPOCONCEP WITH "80" FOR LEGAJO = 264 .AND. LIQUIDA = 2
REPLACE TIPOCONCEP WITH "80" FOR LEGAJO = 294 .AND. LIQUIDA = 2
REPLACE TIPOCONCEP WITH "80" FOR LEGAJO = 422 .AND. LIQUIDA = 2
REPLACE TIPOCONCEP WITH "80" FOR LEGAJO = 433 .AND. LIQUIDA = 2
REPLACE TIPOCONCEP WITH "80" FOR LEGAJO = 613 .AND. LIQUIDA = 2
REPLACE TIPOCONCEP WITH "80" FOR LEGAJO = 620 .AND. LIQUIDA = 2
REPLACE TIPOCONCEP WITH "80" FOR LEGAJO = 674 .AND. LIQUIDA = 2
REPLACE TIPOCONCEP WITH "80" FOR LEGAJO = 823 .AND. LIQUIDA = 2
REPLACE TIPOCONCEP WITH "80" FOR LEGAJO = 906 .AND. LIQUIDA = 2





